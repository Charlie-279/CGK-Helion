# Claude Code Rules

## Auto-Commit and Push

After every file change, you MUST:

1. Stage the changed files: `git add <files>`
2. Commit with a clear message: `git commit -m "description"`
3. Push to GitHub: `git push origin main`
